import streamlit as st

# This sets the metadata of the page (equivalent to <title>)
st.set_page_config(page_title="Prediction Web App")

# This creates the main header (equivalent to <h1>)
st.title("Welcome to Web Page")

# You can add additional content below
st.write("This is a simple Streamlit application converted from your HTML template.")